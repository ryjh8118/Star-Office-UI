# Construction loops

All image-model calls: zero. Model master / image parent lineage: unavailable.
These are code-native SVG iterations, not Multi-turn Editing claims.

| Loop | At most three priorities | Actual work and evidence | Decision |
| --- | --- | --- | --- |
| Baseline | current state, authority, isolation | Production viewed; 4aef2c1 checked against remote; separate worktree and loopback Preview; before screenshots and raw audit | Preserve Office and character identities |
| 1 | terrain/grid, canopy, text clutter | Soft paper strata and grass patches; layered trees; duplicate SVG labels removed; bridge detail and HTML Office entry; real CUA screenshot inspected in session | Better terrain; trees still too regular and actors cross roofs |
| 2 | tree spacing, actor roofs, river end | Jittered sparse trees and shrubs; standees use separated display positions avoiding home silhouettes; river extended to clipping shore | Use revised composition; no change to era formulas or source identities |
| 3 | mobile obstruction, offline image work, measurement accuracy | Office entry moved into sky margin; public thumbnails prebuilt offline; scene-only image checks and explicit daypart queries; true festival QA response fixture | Preserve this checkpoint; full art remains 6/10 and Sunburst-gated |

Final source/module hashes: `asset-manifest.json`. Original identity references:
`reference-pack.json`. Offline portrait source/output hashes:
`../../../backend/renguin_world/art/portraits/manifest.json` from repository root
(manifest physically lives under backend; no runtime static directory exposes it).

The first before audit contained two measurement mistakes: deferred roster
portraits counted as broken and duplicate daypart query parameters. Both were
diagnosed from actual images/DOM, corrected in the harness, and disclosed in the
README. Old records are retained, not silently edited into a PASS.

No independent-model visual review was performed. SELF_REVIEW has not reached
the requested ≥8/10. No user approval is inferred from internal progress.
